1. probably should create stripe_token_charges and it has an order through stripe_token
2. might need to have stripe_refunds unless that is a negative stripe_charge
3. add key prop to react components, can just be map(x,a) where a is the index and use it as the key
